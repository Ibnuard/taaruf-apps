import * as React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const TaarufScreen = () => {
  return (
    <View style={styles.container}>
      <Text>Halo</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default TaarufScreen;
