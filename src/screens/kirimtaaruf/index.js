import * as React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const KirimTaarufScreen = () => {
  return (
    <View style={styles.container}>
      <Text>Gol</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default KirimTaarufScreen;
